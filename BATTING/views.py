from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.
def HIGHESTRUNS(request):
    return HttpResponse('SACHIN TENDULKAR is the highest rungetter with 34,357 runs')